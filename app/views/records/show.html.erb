<p style="color: green"><%= notice %></p>

<%= render @record %>

<div>
  <%= link_to "Edit this record", edit_user_category_record_path(user_id: current_user.id, category_id: params[:category_id], id: @record.id) %>
  <%= link_to "Back to records", user_category_records_path(user_id: current_user.id, category_id: params[:category_id]) %>
  <%= button_to "Destroy this record", user_category_record_path(user_id: params[:user_id],category_id: params[:category_id], id:@record.id ), method: :delete %>
</div>
